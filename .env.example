# Required for live Gemini generation. Without this key, SpekDulu uses the demo provider.
GEMINI_API_KEY=

# Optional model override
GEMINI_MODEL=gemini-2.5-flash

# Optional Browserless / Playwright websocket endpoint for higher-fidelity URL extraction
BROWSERLESS_WS_ENDPOINT=
